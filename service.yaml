---
apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
    <key>: <value> # routes traffic to all pods with this label key-value pair
  ports:
    - port: <container port> # the container listens on port 80
      protocol: TCP
