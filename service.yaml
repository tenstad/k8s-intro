---
apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
    app: website # routes traffic to all pods with this label key-value pair
  ports:
    - port: 80 # the container listens on port 80
      protocol: TCP
